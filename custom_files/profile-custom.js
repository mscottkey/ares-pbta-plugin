import Component from '@ember/component';

export default Component.extend({
  // heroesguild tab data is supplied by the server via the char object
  // No additional client-side logic needed beyond tab selection
});
